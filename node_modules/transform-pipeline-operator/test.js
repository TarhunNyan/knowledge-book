
let { acorn, walkAST, compile } = require('./index');

let acornOptions = {ecmaVersion: 2020};
let res2 = `
let a=100, b=10, d=1;

function func( val ) { return val * 10 }
function okes( val ) { return val + 10 }

c = d |> func( %% ) |> (%%*2); 
`;
// c = a + b + d |> func( %% )|> okes(%%) |> (%% + a); 

let res = compile( res2 );
console.log( res );


